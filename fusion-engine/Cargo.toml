[package]
name = "dfd-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
integrate = "0.3.1"
nalgebra = "0.35.0"
good_lp = {version = "1.15.2", default-features = false, features = ["clarabel"]}
thermo-species = { path = "../../thermo-species" }
cea-rs = { path = "../../cea-rs" }
